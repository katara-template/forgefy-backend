/**
 * @forgefy/ui — shared, platform-agnostic types.
 *
 * Import the components from a platform entry point:
 *   import { VStack, Stagger } from "@forgefy/ui/web";     // Next.js / React DOM
 *   import { VStack, Stagger } from "@forgefy/ui/native";  // React Native
 */
export type {
  Align,
  Justify,
  ResponsiveValueOptions,
  StaggerEffect,
} from "./shared";
