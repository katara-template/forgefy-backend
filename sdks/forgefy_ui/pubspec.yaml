name: forgefy_ui
description: Forgefy layout & animation primitives for Flutter — VStack, HStack, Grid, Responsive, Spacer, Wrap, Scroll, plus baked-in entrance animations.
version: 0.1.0
repository: https://github.com/forgefy/forgefy
issue_tracker: https://github.com/forgefy/forgefy/issues

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
